// Reducer & Adapter
import delegateReducer, { delegateAdapter } from "./slice";

// Actions
import {

} from "./slice";

import { DelegateType } from "./types";

export {
    delegateReducer,
    delegateAdapter,
    DelegateType,
};