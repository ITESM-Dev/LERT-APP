import { Text } from 'native-base';
import theme from './../styles/theme';

const LertText = (props: any) => {
    return (
        <Text color="#14142B" {...props}>
        </Text>
    );
};

export default LertText;